# This file is for reference only.
# The backend service uses backend/render.yaml
# The frontend is deployed on Vercel
# 
# If you want to deploy everything on Render, use the original configuration.
# For split deployment (backend on Render, frontend on Vercel), see DEPLOYMENT_GUIDE_SPLIT.md

